# SponsorsRisk – Sponsor-centric Clinical Trial Risk Prediction

## Setup

```bash
# Clone repo and set up environment
python3 -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt
